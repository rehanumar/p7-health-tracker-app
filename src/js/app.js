/*global $ */
/*jshint unused:false */
var app = app || {};

/* global variables or constants can be delcared here! */

$(function () {
	'use strict';

	// kick things off by creating the `App`
	new app.AppView();
});
